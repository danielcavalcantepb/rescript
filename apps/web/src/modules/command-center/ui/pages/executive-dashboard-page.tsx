import { useQuery } from '@tanstack/react-query'
import { useOrganization } from '#/platform/organization/organization-context'
import { getExecutiveDashboard } from '../executive-dashboard-api'

type DashboardData = { cards: Record<string, number>; revenue: Array<{ date: string; value: number }>; topProducts: Array<{ product: string; quantity: number; value: number }>; stock: { quantity: number | null; value: number | null; lowStock: number | null; withoutMovement: number | null } }
const money = (value: number) => `R$ ${value.toLocaleString('pt-BR', { minimumFractionDigits: 2 })}`

export function ExecutiveDashboardPage() {
  const { currentOrganization } = useOrganization()
  const query = useQuery({ queryKey: ['executive-dashboard', currentOrganization?.id], queryFn: () => getExecutiveDashboard({ data: { organizationId: currentOrganization!.id, period: '30d' } }), enabled: Boolean(currentOrganization?.id) })
  if (query.isLoading) return <main className="min-h-full bg-[#101314] p-8 text-sm text-[#9aa6a0]">Carregando visão executiva…</main>
  if (query.isError) return <main className="min-h-full bg-[#101314] p-8 text-sm text-red-300">Não foi possível carregar o dashboard.</main>
  const data = query.data as DashboardData | undefined
  if (!data) return <main className="min-h-full bg-[#101314] p-8 text-sm text-[#9aa6a0]">Nenhum dado disponível.</main>
  const cards: Array<[string, number, boolean]> = [['Faturamento hoje', data.cards.revenueToday, true], ['Faturamento do mês', data.cards.revenueMonth, true], ['Vendas do mês', data.cards.salesMonth, false], ['Ticket médio', data.cards.averageTicket, true], ['Clientes', data.cards.customers, false], ['Produtos', data.cards.products, false], ['Contas a receber', data.cards.receivables, true], ['Contas a pagar', data.cards.payables, true]]
  const maxRevenue = Math.max(...data.revenue.map((row) => row.value), 1)
  return <main className="min-h-full bg-[#101314] p-4 text-[#f3f7f4] sm:p-6 lg:p-8"><div className="mx-auto max-w-[1500px] space-y-6"><header className="flex flex-col gap-4 border-b border-white/10 pb-5 sm:flex-row sm:items-end sm:justify-between"><div><p className="mb-2 text-[11px] font-semibold uppercase tracking-[0.22em] text-[#8eea49]">Visão executiva · últimos 30 dias</p><h1 className="text-3xl font-semibold tracking-tight">Bom dia. Aqui está sua operação.</h1><p className="mt-2 text-sm text-[#9aa6a0]">Uma leitura objetiva para decidir o próximo movimento.</p></div><button className="rounded-lg border border-white/15 px-4 py-2 text-sm text-[#d8e2db] transition hover:border-[#8eea49] hover:text-[#8eea49]" onClick={() => void query.refetch()} disabled={query.isFetching}>{query.isFetching ? 'Atualizando…' : 'Atualizar dados'}</button></header><section className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{cards.map(([label, value, isMoney], index) => <div key={label} className="relative overflow-hidden rounded-xl border border-white/10 bg-[#1a1e20] p-4 shadow-[0_12px_35px_rgba(0,0,0,.18)]"><div className="absolute right-0 top-0 h-20 w-20 rounded-full bg-[#8eea49]/[0.07] blur-2xl"/><div className="relative"><div className="flex items-center justify-between text-xs text-[#9aa6a0]"><span>{label}</span><span className="rounded-full bg-white/[0.06] px-2 py-1 text-[10px] text-[#8eea49]">{index < 4 ? '30d' : 'base'}</span></div><div className="mt-4 text-2xl font-semibold tracking-tight">{isMoney ? money(Number(value)) : Number(value).toLocaleString('pt-BR')}</div></div></div>)}</section><section className="grid gap-5 xl:grid-cols-[1.45fr_.85fr]"><div className="rounded-xl border border-white/10 bg-[#1a1e20] p-5"><div className="flex items-center justify-between"><div><h2 className="text-lg font-medium">Faturamento</h2><p className="mt-1 text-xs text-[#9aa6a0]">Evolução diária do período selecionado</p></div><span className="rounded-full border border-[#8eea49]/30 px-3 py-1 text-xs text-[#8eea49]">30 dias</span></div>{data.revenue.length ? <div className="mt-8 flex h-48 items-end gap-1.5 sm:gap-2">{data.revenue.map((row) => <div key={row.date} className="group flex min-w-0 flex-1 flex-col items-center justify-end gap-2"><div className="w-full rounded-t-md bg-gradient-to-t from-[#52bc42] to-[#c4ff67] opacity-80 transition group-hover:opacity-100" style={{ height: `${Math.max((row.value / maxRevenue) * 100, 4)}%` }} title={`${row.date}: ${money(Number(row.value))}`} /><span className="hidden text-[9px] text-[#718078] sm:block">{row.date.slice(5)}</span></div>)}</div> : <div className="flex h-48 items-center justify-center text-sm text-[#9aa6a0]">Nenhuma venda confirmada no período.</div>}</div><div className="rounded-xl border border-white/10 bg-[#1a1e20] p-5"><h2 className="text-lg font-medium">Produtos mais vendidos</h2><p className="mt-1 text-xs text-[#9aa6a0]">Top 10 por valor vendido</p><div className="mt-5 space-y-3">{data.topProducts.length ? data.topProducts.map((row, index) => <div key={row.product} className="flex items-center gap-3"><span className="flex h-6 w-6 items-center justify-center rounded-full bg-white/[0.06] text-[11px] text-[#8eea49]">{index + 1}</span><div className="min-w-0 flex-1"><div className="truncate text-sm">{row.product}</div><div className="mt-1 h-1 rounded-full bg-white/10"><div className="h-1 rounded-full bg-[#8eea49]" style={{ width: `${Math.max((row.value / Number(data.topProducts[0].value || 1)) * 100, 5)}%` }} /></div></div><span className="text-xs text-[#cbd6ce]">{money(Number(row.value))}</span></div>) : <p className="py-8 text-sm text-[#9aa6a0]">Nenhuma venda confirmada no período.</p>}</div></div></section><section className="grid gap-5 lg:grid-cols-[1fr_1fr_.8fr]"><div className="rounded-xl border border-white/10 bg-[#1a1e20] p-5"><h2 className="text-lg font-medium">Estoque</h2><p className="mt-1 text-xs text-[#9aa6a0]">Posição operacional atual</p><div className="mt-6 text-3xl font-semibold">{Number(data.stock.quantity ?? 0).toLocaleString('pt-BR')} <span className="text-sm font-normal text-[#9aa6a0]">unidades</span></div><p className="mt-3 text-xs text-[#9aa6a0]">Valor financeiro ainda não disponível no domínio atual.</p></div><div className="rounded-xl border border-white/10 bg-[#1a1e20] p-5"><h2 className="text-lg font-medium">Saúde financeira</h2><p className="mt-1 text-xs text-[#9aa6a0]">Obrigações já persistidas</p><div className="mt-5 grid grid-cols-2 gap-3"><Metric label="A receber" value={money(Number(data.cards.receivables))} /><Metric label="A pagar" value={money(Number(data.cards.payables))} /></div></div><div className="rounded-xl border border-[#8eea49]/30 bg-gradient-to-br from-[#1e3328] to-[#17201c] p-5"><div className="text-xs uppercase tracking-[0.18em] text-[#8eea49]">Próximo passo</div><h2 className="mt-3 text-xl font-medium">Mantenha o contexto perto da decisão.</h2><p className="mt-2 text-xs leading-relaxed text-[#b7c7bc]">Use os módulos ao lado para aprofundar cada sinal operacional.</p></div></section></div></main>
}
function Metric({ label, value }: { label: string; value: string }) { return <div className="rounded-lg bg-white/[0.05] p-3"><div className="text-xs text-[#9aa6a0]">{label}</div><div className="mt-1 text-sm font-medium">{value}</div></div> }
