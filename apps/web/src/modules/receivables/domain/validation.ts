import type{CreateReceivableInput}from'./types'
const units=(v:string)=>{if(!/^\d+(\.\d{1,4})?$/.test(v))throw new Error('invalid_amount');return BigInt(v.split('.')[0]!)*10000n+BigInt((v.split('.')[1]??'').padEnd(4,'0'))}
export function validateReceivable(input:CreateReceivableInput){if(!input.customerId)throw new Error('customer_required');if(!input.issueDate||!input.dueDate||input.dueDate<input.issueDate)throw new Error('invalid_dates');const total=units(input.totalAmount);if(total<=0n||input.installments.length===0)throw new Error('invalid_amount');if(input.installments.some(i=>!i.dueDate||i.dueDate<input.issueDate||units(i.amount)<=0n))throw new Error('invalid_installment');if(input.installments.reduce((s,i)=>s+units(i.amount),0n)!==total)throw new Error('installment_total_mismatch')}
