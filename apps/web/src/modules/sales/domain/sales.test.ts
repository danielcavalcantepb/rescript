import{describe,expect,it}from'vitest';import{assertQuotationTransition,assertSalesOrderTransition,quotationStatusLabel,salesOrderStatusLabel}from'./lifecycle';import{validateSalesItems}from'./validation'
describe('sales foundation',()=>{it('validates item totals and currency-safe decimal payloads',()=>expect(()=>validateSalesItems([{variantId:'v',quantity:'2',unitPrice:'10.25',discount:'1'}])).not.toThrow());it('accepts price-list snapshots while preserving item validation',()=>expect(()=>validateSalesItems([{variantId:'v',quantity:'2',unitPrice:'0',discount:'0',priceListId:'pl'}])).not.toThrow());it('rejects invalid item quantities and negative-like payloads',()=>{expect(()=>validateSalesItems([{variantId:'',quantity:'0',unitPrice:'1'}])).toThrow('invalid_item');expect(()=>validateSalesItems([{variantId:'v',quantity:'1',unitPrice:'10',discount:'11'}])).toThrow('invalid_item')});it('enforces quotation lifecycle',()=>{expect(()=>assertQuotationTransition('draft','sent')).not.toThrow();expect(()=>assertQuotationTransition('sent','approved')).not.toThrow();expect(()=>assertQuotationTransition('approved','archived')).not.toThrow();expect(()=>assertQuotationTransition('draft','approved')).toThrow('invalid_quotation_transition')});it('enforces order lifecycle',()=>{expect(()=>assertSalesOrderTransition('draft','confirmed')).not.toThrow();expect(()=>assertSalesOrderTransition('confirmed','cancelled')).not.toThrow();expect(()=>assertSalesOrderTransition('cancelled','archived')).not.toThrow();expect(()=>assertSalesOrderTransition('confirmed','archived')).toThrow('invalid_sales_order_transition')});it('keeps operational labels stable for workspaces',()=>{expect(quotationStatusLabel('sent')).toBe('Enviada');expect(salesOrderStatusLabel('confirmed')).toBe('Confirmado')})})
